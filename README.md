# learning-playwright

This repository is primarily a Playwright sandbox.

## Playwright

- `npm test`: run Playwright tests
- `npm run test:ui`: open Playwright UI mode
- `npm run typecheck`: TypeScript typecheck for test/page/fixture code (no JS emit)
